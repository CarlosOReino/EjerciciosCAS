/*Haz una función que obtenga el maximo numero de un array*/
const array = [-1, 5, 25, 2];
function devolver(array, pos, max) {
    if( array == pos == max){
        
    }
}
console.log(devolver(array));
